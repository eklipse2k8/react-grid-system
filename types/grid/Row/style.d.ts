import { RowProps } from './Row';
import type * as CSSUtil from '@stitches/react/types/css-util';
export default function createRowStyle({ gutterWidth, align, justify, debug, style, direction, wrap, }: RowProps): CSSUtil.CSS;
